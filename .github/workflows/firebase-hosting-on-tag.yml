# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  workflow_dispatch:
    branches:
      - master
    inputs:
      enviroment: 
        description: env"
        required: true
        default: "dev"
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.enviroment}}
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          - name: condition1
            if: "contains(secrets.FIREBASE_SERVICE_ACCOUNT_SAMPLE_DEV_DC666,'dev')"
              firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SAMPLE_DEV_DC666}}'
          - name: condition2
            if: "contains(secrets.FIREBASE_SERVICE_ACCOUNT_SAMPLE_DEV_DC666,'prod')"
              firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SAMPLE_ABAD6}}'
          channelId: live
          projectId: sample-dev-dc666
